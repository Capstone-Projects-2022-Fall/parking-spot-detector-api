import React from 'react';
import { HeaderMain } from './header.styles';

const Header = () => {
    return (
        <HeaderMain>
            Header of Website
        </HeaderMain>
    );
};

export default Header;
