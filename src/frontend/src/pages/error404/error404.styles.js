import styled from 'styled-components';
import * as colors from '../../constants/colors';

export const Error404Main = styled.div`
    padding: 1em;
    margin: 0 auto;
    width: fit-content;
    color: ${colors.customWhite};

    background-color: ${colors.normalPurple};
    border-radius: 1em;

    user-select: none;
`;