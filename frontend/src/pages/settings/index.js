import React from 'react';
import {
    SettingsContainer
} from './settings.styles';

export default function SettingsPage() {
    return (
        <SettingsContainer>
            TEstTTT
        </SettingsContainer>
    );
}